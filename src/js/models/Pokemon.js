export class Pokemon {
  constructor(name, number, type, types, photo, height, weight) {
    this.name = name;
    this.number = number;
    this.type = type;
    this.types = types;
    this.photo = photo;
    this.height = height;
    this.weight = weight;
  }
}
